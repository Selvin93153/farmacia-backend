services:
  backend:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - .:/app
      - backend_node_modules:/app/node_modules
    command: tail -f /dev/null

volumes:
  backend_node_modules: